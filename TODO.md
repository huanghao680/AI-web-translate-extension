# TODO

## 已知 Bug

- 切换原文/译文后，划选翻译的结果无法被 toggle 恢复（选区文本节点被修改但快照被整体替换覆盖）
- 块选择翻译的监听器在 `stopBlockSelection()` 中通过 `removeDocumentListeners()` 整体移除，但会误清其他功能注册的监听器
- OpenWRT 等 DokuWiki 页面中 `<code>` 内联元素导致段落上下文断裂

## 待优化

- 整页翻译的进度条在翻译完成后自动消失，但用户无法查看历史翻译记录
- 错误日志仅保留 200 条，无导出功能
- 翻译风格的部分提示词可继续丰富（正式/简洁/学术）
- 弹窗暗色模式下部分元素的对比度可调优
- 内容脚本的 `_listeners` 数组在 `stopBlockSelection` 被整体清空，影响后续监听器注册

## 功能建议

- 翻译特定容器（如 `#content`、`article`）而非整个 `<body>`
- 排除特定 CSS 选择器的元素（用户可配置）
- 快捷键支持（如 Alt+T 翻译整页）
- 划选翻译的选区范围可微调（类似块选择的滚轮/方向键调整）
- 翻译结果对比模式（原文/译文左右分栏）
